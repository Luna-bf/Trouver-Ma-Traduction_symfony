{% extends "base.html.twig" %}

{% block title %}Publier une traduction - Trouver Ma Traduction
{% endblock %}

{% block body %}
	<body class="forms-body">
		{% block main %}
			<main class="form-container d-flex">
				<div class="form-parent d-flex">
					{{ form_start(translationForm, {'attr': {'class': 'd-flex'}}) }}
						{{ form_row(translationForm.name) }}
						{{ form_row(translationForm.translation_file_name) }}
						{{ form_row(translationForm.translation_type) }}
						{{ form_row(translationForm.translation_style) }}
						{{ form_row(translationForm.author) }}
						{{ form_row(translationForm.language) }}
						{{ form_row(translationForm.save) }}
					{{ form_end(translationForm, {'render_rest': false}) }}
				{# div ouvrante ici #}
					{# <div class="w-50-percent d-i-flex form-input-parent"> #}
					{# <label for="translation" class="">Votre fichier</label> #}
					{# {{ form_label(translationForm.name, 'My Custom Task Label', {'attr': {'class': 'w-50-percent mb-10 mt-first-label sign-label'}}) }} #}
					{# {{ form_label({'attr': {'class': 'w-50-percent mb-10 mt-first-label sign-label'}}) }} #}
					{# {{ form_row(translationForm.name, { 'label': 'Votre fichier' }, {'attr': {'class': 'form-input <w-50-percent mb-10 mt-first-label sign-label>'}}) }} #}
					
					
					{# {{ form_label(translationForm.translation_file_name, { 'label': 'Votre fichier', 'attr': 'class': 'w-50-percent mb-10 mt-first-label sign-label'}) }} #}
					
					{# {{ form_label(translationForm.translation_file_name, { 'label': 'Votre fichier', { 'attr': {'class': 'w-50-percent mb-10 mt-first-label sign-label'}}}) }} #}
					{# <label for="" class="w-50-percent mb-10 mt-first-label sign-label">Testing</label> #}
					{# {{ form_label(translationForm.name, '', { 'label_attr': {'class': 'w-50-percent mb-10 mt-first-label sign-label'} }) }} #}
					{# {{ form_row(translationForm.name, {'attr': {'class': 'form-input'}}) }}
				{# div fermante ici #}

				{# div ouvrante ici #}
					{# {{ form_row(translationForm.translation_type, { 'label': 'Publier', 'attr': {'class': 'w-50-percent mb-10 mt-first-label sign-label'}}, {'attr': {'class': 'form-input'}}) }} #}
				{# div fermante ici #}

				{# div ouvrante ici #}
					{# {{ form_row(translationForm.translation_style, {'attr': {'class': 'form-input'}}) }} #}
				{# div fermante ici #}

				{# div ouvrante ici #}
					{# {{ form_row(translationForm.author, {'attr': {'class': 'form-input'}}) }} #}
				{# div fermante ici #}

				{# div ouvrante ici #}
					{# {{ form_row(translationForm.language, {'attr': {'class': 'form-input'}}) }}#}
				{# div fermante ici #}

				{# div ouvrante ici #}
					{# {{ form_row(translationForm.createdAt, {'attr': {'class': 'form-input'}}) }} #}
				{# div fermante ici #}

				{# div ouvrante ici #}
					{# {{ form_row(translationForm.save, { 'label': 'Publier' }) }}  #}
				{# div fermante ici #}
			</div>
			
			{# {{ form_end(translationForm) }} #}
		</main>
	{% endblock %}

		{##}
		<!-- <script src="../assets/js/file-visualiser.js"></script> -->
	</body>
{% endblock %}
